import {
    ImageUpload
} from "../components"

export const AddPage = function() {
    return (
        <div className="IndexPage">
            add pages

            <ImageUpload open={true} />
        </div>
    );
}